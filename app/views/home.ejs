<!DOCTYPE html>
<html>
    <head>
        <title> VizSim </title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700;900&display=swap" 
        rel="stylesheet">   
        <link type="text/css" rel="stylesheet" href="/css/style.css" />
    </head>
    <body>


        <div class="container"> 
            <h1>Viz<span class="color-span">Sim </span> </h1>
            <div class="sample">

                <p>Select the most similar images to the following image </p>

                <% if (message == 'Success!') { %>
                    <div class="alert success"><%= message %></div>
                <% } %>

                <img class="query-image" src="images/<%= query['path'] %>" width=150 height=150/>

                <form action="/vote" method="POST">
                    <div class="row-1">

                        <% for(var i=0; i < 5; i++) { %>
                            <input name="<%= samples[i]['id'] %>" type="checkbox" id="<%= samples[i]['id'] %>" />
                                <label for="<%= samples[i]['id'] %>">
                                    <img src="images/<%= samples[i]['path'] %>" width=100 height=100/>
                                </label>
                         <% } %>

                    </div>
                    <div class="row-2">

                         <% for(var i=5; i < 10; i++) { %>
                            <input name="<%= samples[i]['id'] %>" type="checkbox" id="<%= samples[i]['id'] %>" />
                                <label for="<%= samples[i]['id'] %>">
                                    <img src="images/<%= samples[i]['path'] %>" width=100 height=100/>
                                </label>
                         <% } %>

                    </div>

                    <input name="query_<%= query['id'] %>" type="hidden" />      
                    <input name="user_<%= user_id %>" type="hidden" />                 

                    <button type="submit"">SUBMIT</button>
                </form>
            </div>
        </div>
    </body>
</html>